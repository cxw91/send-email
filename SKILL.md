---
name: send-email
description: 交互式邮件群发工具 - 显示联系人列表供选择，支持动态添加并保存到Excel，当用户需要发送邮件、邮件发送，或提到“发邮件”、“邮件群发”、“给XX发邮件”时，自动激活此技能。
allowed-tools: Read, Write, Bash
author: cxw
---

# 交互式邮件群发 Skill

## 目标
提供一个交互式界面，让用户从Excel联系人列表中选择收件人，并支持添加新联系人，最后发送个性化邮件。

## 数据文件
默认 `docs/contacts.xlsx`（不存在则创建，列：姓名, 邮箱）。

## 执行步骤

### 1. 初始化
- 检查 Excel 文件是否存在，若不存在则创建表头（姓名, 邮箱）。
- 读取所有联系人，存储为列表。

### 1.5 指定收件人快捷匹配（用户直接点名时优先）
若用户在请求中明确点名某位收件人，**不要先弹出列表选择面板**，按以下流程处理：
1. 在内存中的联系人列表里，按「姓名」做精确匹配，失败时再做包含/模糊匹配。
2. **命中**：直接带出该联系人（姓名 + 邮箱），跳过步骤2的弹窗，直接进入步骤4（编辑邮件内容）→ 步骤5（发送）。
3. **未命中**：进入新增流程——
   - 提示用户提供该收件人的邮箱（如「请提供 XXX 的邮箱」）。
   - 拿到邮箱后，先检查该邮箱是否已绑定表中**其他**联系人：
     - 若已存在 → 提示「该邮箱已绑定到 XXX，是否仍以此名义（被点名人）发送？」；确认后发送，不重复建行。
     - 若不存在 → 将「姓名 + 邮箱」追加写入 `docs/contacts.xlsx`，再发送。
4. 仅当用户**未点名**、需要多选或浏览列表时，才走步骤2的弹窗选择面板。

### 2. 显示列表（交互式弹窗 · 勾选）
- 将每位联系人作为一个可勾选项展示（编号.姓名 + 邮箱）。
- 必须用 AskUserQuestion 且 multiSelect=true（多选勾选）。
- 选项：每位联系人一项（标签=编号.姓名，描述=邮箱）或者可选 all 全选。

### 3. 处理用户选择

- 如果 `selected` 为空，提示“请至少选择一位收件人”，回到步骤2。
- 否则进入步骤4。

### 4. 编辑邮件内容
- 询问邮件标题（或给出默认建议）。
- 询问邮件正文模板，支持变量替换，如 `{姓名}` 会自动替换为联系人的姓名。
- 可选：询问是否需要添加附件（若需要，请用户提供文件路径）。
- **用 AskUserQuestion 弹出预览确认面板**，展示第一封邮件示例（标题 + 正文），让用户确认或要求修改。

### 5. 发送邮件
- 使用已配置的邮件发送能力（Agent Mail 或 SMTP）。
- **若 Agent Mail 返回 `CONFIRMATION_REQUIRED`：必须用 AskUserQuestion 展示 operation_summary（发件人 / 收件人 / 主题 / 附件数），请用户确认；拿到 `confirmation_token` 后带 token 重试发送。**
- 对 `selected` 中的每位联系人，生成个性化邮件（替换 `{姓名}` 等变量）。
- 每封邮件间隔 0.5 秒。
- 发送完成后，报告成功/失败列表。

### 6. 结束
- 提示“发送完成！”，并询问是否继续发送（回到步骤2）或退出。

## 注意事项
- Excel 文件路径可在首次使用时由用户指定，之后记住。
- 添加新联系人时，检查是否已存在相同邮箱，若存在则提示“该邮箱已存在，是否覆盖？”。
- 邮件内容中的变量支持 `{姓名}`、`{邮箱}` 以及 Excel 中的其他列（如有）。
- **点名即查**：用户若直接点名收件人，先查 `docs/contacts.xlsx`，命中走发送、未命中走添加（见步骤 1.5）；不要一上来就弹列表。
- 点名收件人的邮箱若与表中其他人重复，发送前需向用户说明并确认，避免重复投递或误投。
